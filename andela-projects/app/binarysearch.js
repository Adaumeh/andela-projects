module.exports = outcome;

const outcome = {var a=[start], b=start;
  while(b<stop){b+=step;a.push(b)}
  return a
  };

outcome.toTwenty = [].toTwenty();
outcome.toForty = [].toForty();
outcome.toOneThousand = [].toOneThousand();